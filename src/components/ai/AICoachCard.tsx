/**
 * AI Coach Card - DISABLED (AI-free version)
 * This component is hidden until AI features are re-enabled
 */
export const AICoachCard = () => {
  // AI features are disabled - return null to hide the card
  return null;
};
